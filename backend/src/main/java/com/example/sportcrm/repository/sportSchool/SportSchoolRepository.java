package com.example.sportcrm.repository.sportSchool;

import com.example.sportcrm.model.SportSchool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SportSchoolRepository extends JpaRepository<SportSchool, Long> {
}
